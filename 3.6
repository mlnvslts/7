declare -a configs
configs[home]="~/.bashrc-home"
configs[work]="~/.bashrc-work"
if ["$1"=="home"] || ["$1"=="work"]; then
  cp ~/"${configs[$1]}" ~/.bashrc
  source ~/.bashrc
else
    echo "файл не найден"
fi
